package com.crud.demo.entity;

import java.util.List;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * The Class Product.
 */
//@Entity
/*
 * @Data
 * 
 * @AllArgsConstructor
 * 
 * @NoArgsConstructor
 */
public class Product {

	/** The id. */
	/*
	 * @Id
	 * 
	 * @Column(name = "id") private int id;
	 * 
	 *//** The title. */
	/*
	 * @Column(name = "title") private String title;
	 * 
	 *//** The description. */
	/*
	 * @Column(name = "description") private String description;
	 * 
	 *//** The price. */
	/*
	 * @Column(name = "title") private double price;
	 * 
	 *//** The discount percentage. */
	/*
	 * @Column(name = "discountPercentage") private double discountPercentage;
	 * 
	 *//** The rating. */
	/*
	 * @Column(name = "rating") private double rating;
	 * 
	 *//** The stock. */
	/*
	 * @Column(name = "stock") private double stock;
	 * 
	 *//** The brand. */
	/*
	 * @Column(name = "brand") private String brand;
	 * 
	 *//** The category. */
	/*
	 * @Column(name = "category") private String category;
	 * 
	 *//** The thumnail. */
	/*
	 * @Column(name = "thumnail") private String thumnail;
	 * 
	 *//** The images. *//*
							 * @Column(name = "images") private List<String> images;
							 */

}
